client folder : interactive part ; 
server folder : handling 
     > models: schemas 
     > routes: redirecting
     > utils: tokens
     > env: environment variables
     > middlewares: authentication
     > controllers: functions